// Area error codes
// Единый источник ошибок для одной области { x, y, w, h }.

export const AREA_ERRORS = {
  INVALID_NUMBERS: "INVALID_NUMBERS",
  INVALID_INTEGER: "INVALID_INTEGER",
  INVALID_SIZE: "INVALID_SIZE"
};
