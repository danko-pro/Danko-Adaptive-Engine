// Default grid rules
// Единственный источник базовых правил adaptive-engine.

export const defaultGridRules = {
  minColumns: 30,
  minVisibleColumns: 8,
  maxColumns: 80,
  minRows: 30,
  minVisibleRows: 18,
  maxRows: 60,
  fitPadding: 2,
  minCellSize: 16,
  maxCellSize: 30
};
